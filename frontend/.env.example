# Deployed read API URL — used for both local dev (npm run dev) and production build.
# Set this to the ApiUrl from CDK deploy output, e.g.:
#   https://xxxxxxxxxx.execute-api.ca-central-1.amazonaws.com
VITE_API_URL=
